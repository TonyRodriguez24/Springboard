import { createContext } from "react";

const UserContext = createContext();

export default UserContext;

//this creates an empty container almost and we can share that value without props in manually